Add: Route waypoint generation system 

- moved haversine distance calculation from csv_reader.c to route_planning.c
- Add generate_waypoints() using spherical interpolation (slerp)
- Support ICAO airport code lookup (10 major airports included)
- Generate waypoints at configurable intervals (default: 50km)

Example: CYOW-->CYYZ generates 8 waypoints over 350km route
This enables multi-point lightning risk assessment along flight paths.