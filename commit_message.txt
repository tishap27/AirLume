feat: add ML model training pipeline with sample data

Implements Gradient Boosting classifier training,cross-validation, and performance metrics. Includes sample data generator(500 fake scenarios) for testing before real flight data available.