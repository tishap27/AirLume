feat: connect JSF frontend to React globe via URL params and fix CORS
- Add openGlobe() method to FlightController to redirect to React app
  with origin, destination, flightLevel as URL params
- Update App.jsx to read URL params on mount (URLSearchParams)

CORS fixes:
- Add CorsFilter.java - appends CORS headers to all responses
- Add CorsPreflightResource.java - handles OPTIONS preflight requests
- Add ApplicationConfig.java with @ApplicationPath("resources") to
  register all JAX-RS resources and providers